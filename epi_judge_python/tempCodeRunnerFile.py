            # if weight <= cap:
            #     DP[item_idx, cap] = max(
            #         DP[item_idx-1, cap], value + DP[item_idx-1, cap-weight])
            # else:
            #     DP[item_idx, cap] = DP[item_idx-1, cap]